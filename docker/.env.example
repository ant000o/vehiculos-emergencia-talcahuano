# Copiar este archivo a docker/.env y ajustar valores localmente.
# NUNCA subir el archivo .env real al repositorio.

DB_USER=bomberos_user
DB_PASSWORD=changeme
DB_NAME=bomberos_talcahuano
DB_PORT=5432
ADMINER_PORT=8080
BACKEND_PORT=3000

# Solo requeridas para docker-compose.prod.yml
JWT_SECRET=changeme_prod_secret
JWT_REFRESH_SECRET=changeme_prod_refresh_secret
